//We are going to create a API Route that could act like a backend and intercept different request method and execute calls to a db (endpoint)
//the name of the folder it has to be obligatoy called api and been inside of pages
function handler(req, res) {
    if (req.method === 'POST') {

    }
}
export default handler;